/** Global gameplay settings — tweak here, applies to every level. */
export const SETTINGS = {
  /** Max layers the conveyor can hold at once (fixed slot count, global). */
  conveyorCapacity: 8,
  /** Conveyor travel speed in world units per second. */
  conveyorSpeed: 3.2,
  /** Default layers-per-container shown in the editor setup step. */
  defaultCapacity: 10,
};
